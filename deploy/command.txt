
# 启动
sudo systemctl start suc_arena_web.service

# 停止
sudo systemctl stop suc_arena_web.service

# 重启
sudo systemctl restart suc_arena_web.service

# 查看状态
sudo systemctl status suc_arena_web.service

# 设为开机启动
sudo systemctl enable suc_arena_web.service



# 查找npm安装路径
which npm

# 查看npm版本
npm -v

# 查看node版本
node -v
